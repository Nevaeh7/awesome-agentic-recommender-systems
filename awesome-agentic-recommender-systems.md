# Awesome Agentic Recommender Systems

The curated list has moved to [link](README.md).

The README keeps paper URLs hidden behind clickable `link` labels, code URLs hidden behind clickable `code` labels, and uses `N/A` when no public code is available.
